name: core_domain
description: Core domain layer with base entities, failures, and value objects
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  freezed_annotation: ^2.4.1
  fpdart: ^1.1.0
  equatable: ^2.0.5

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.5
  test: ^1.24.0
