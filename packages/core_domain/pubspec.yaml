name: core_domain
description: Core domain layer - entities, failures, use cases, value objects
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  freezed_annotation: ^2.4.1
  fpdart: ^1.1.0
  equatable: ^2.0.7
  meta: ^1.15.0

dev_dependencies:
  build_runner: ^2.4.8
  freezed: ^2.5.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
