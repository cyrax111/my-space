name: core_logging
description: Logging abstraction for the application
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  logger: ^2.0.2
  injectable: ^2.3.2

dev_dependencies:
  test: ^1.24.0
