name: core_analytics
description: Analytics abstraction with pluggable providers
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
