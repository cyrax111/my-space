name: core_storage
description: Storage abstraction with SQLite support using drift
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.8.3
  injectable: ^2.3.2

dev_dependencies:
  drift_dev: ^2.14.0
  build_runner: ^2.4.6
  test: ^1.24.0
